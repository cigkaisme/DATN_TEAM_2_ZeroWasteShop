import { SliderMarquee } from './slider-marquee';

describe('SliderMarquee', () => {
  it('should create an instance', () => {
    const directive = new SliderMarquee();
    expect(directive).toBeTruthy();
  });
});
